hidapi
# NOTE: must be `hidapi`, NOT `hid` — the `hid` package imports as `hid` too but needs a
# system hidapi library (fails on macOS with ImportError about libhidapi*.dylib).
platformdirs
pystray
pillow
certifi

# Windows-specific
ctwin32 ; sys_platform == 'win32'
pygetwindow ; sys_platform == 'win32'
pyinstaller ; sys_platform == 'win32'

# macOS-specific
# Pinned: pyobjc 12.x requires macOS 15.7+ and abort()s on older 15.x (e.g. 15.6).
# 10.3 is the newest release that supports a broad range of macOS versions.
pyobjc==10.3 ; sys_platform == 'darwin'  # Needed for AppKit and Quartz

# Linux-specific
ewmh ; sys_platform == 'linux'
psutil ; sys_platform == 'linux'
python-xlib ; sys_platform == 'linux'
